@page
@model FoodTracker_APP.Pages.BlacklistFoods.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Blacklist</h1>

<p>
    <a asp-page="Create">Add new Food to your Blacklist: </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Blacklist[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Blacklist[0].DateHour)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Blacklist) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateHour)
            </td>
            <td>                
                <a asp-page="./Delete" asp-route-id="@item.Id"onclick="return confirm("Are you sure you want to Delete the item from the Blacklist?")">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
